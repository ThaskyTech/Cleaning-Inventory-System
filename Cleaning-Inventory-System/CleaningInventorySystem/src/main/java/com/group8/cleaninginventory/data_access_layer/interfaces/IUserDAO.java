package com.group8.cleaninginventory.data_access_layer.interfaces;

// Defines the data access contract for user accounts (login lookup, registration, duplicate checks).
public interface IUserDAO {
}
